Signature store for the CLA Assistant workflow (.github/workflows/cla.yml).

The action writes signatures/version1/cla.json here. Nothing else belongs on
this branch, and it must not be protected — the action commits to it.